# Amplify API Configuration
# All email notifications will be sent FROM the email associated with this Amplify account.
# Make sure Outlook integration is enabled in Amplify.
REACT_APP_AMPLIFY_API_KEY=amplify_key

# Email Configuration for CCC Admin 
# Receives all notifications during "CCC Review" and "FIS Entry" steps, as well as any rejections.
CCC_FACULTY_EMAIL=faculty_email

# Email to be used in test files for email notifications (sends all test applicant emails here)
TEST_EMAIL=your_email

# URL configuration for deployment
FRONTEND_URL=frontend_url